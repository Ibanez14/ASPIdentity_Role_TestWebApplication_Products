Done

 - Created Identity with int Id, the same for Role
 - Added Nav properties to AppUser so that to get Claims and so on
 - Configured New AppUser and AppRole and since relations are many to many, I set them onModelCreating
  - Lazy loading installed
